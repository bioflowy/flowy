{
  "name": "flowy",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/cwl-ts-auto;npm run build --workspace=packages/server;npm run build --workspace=packages/client;npm run build --workspace=packages/worker"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
